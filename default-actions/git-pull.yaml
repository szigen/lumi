id: git-pull
label: Git Pull
icon: GitBranch
scope: user
claude:
  model: sonnet
  allowedTools:
    - "Bash(git *)"
    - "Read"
steps:
  - type: write
    content: "claude \"Pull the latest changes from the remote repository and resolve any conflicts.\"\r"
